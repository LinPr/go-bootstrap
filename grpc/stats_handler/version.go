package stats_handler

var (
	// Version is the current release version of the go-bootstrap/http library.
	// Set via ldflags: go build -ldflags "-X github.com/LinPr/go-bootstrap/http.Version=$(git describe --tags --always --dirty)"
	Version = "v0.1.0"
)
